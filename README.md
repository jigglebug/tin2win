If you want to make it into a exe file (for some reason even though there's a release for windows) you need to use pyinstaller or whatever app the ports python code to a executeable

tin2win is a program that uses WSL and wslG to make wslG think that the panels in Tint2 are seprate windows and render them as a program without a icon (resulting in what i call "baby tux and big tux")
